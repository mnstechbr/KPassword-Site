{
  "name": "kpassword-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run sync:releases",
    "dev": "astro dev",
    "sync:releases": "node ./scripts/sync-github-releases.mjs",
    "prebuild": "npm run sync:releases",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
